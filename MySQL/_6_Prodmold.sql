-- Step 5: Create a table for terms JSON array
CREATE TABLE terms (
    tid VARCHAR(10),
    code VARCHAR(50),
    unitatea_mas VARCHAR(10),
    vid VARCHAR(10),
    name VARCHAR(255),
    description VARCHAR(255),
    format VARCHAR(255),
    weight VARCHAR(10),
    language VARCHAR(10)
);
Sarcina -
 --------Trebuie de actualizat clasificatorul  PRODMOLD-A pe edecbns-test.sfs.md

 Se da:
 1. clasificatorul  nou este in fisier excel
 2. clasificatorul pe edecbns-test.sfs.md este in format  json - Denumirea este -  prodmold
 3. Pe  edecbns-test.sfs.md clasificatorul prodmold se poate exporta in format  json




1. Trebuie sa creiezi tabel in Oracke 

I have this table in MySQL 

CREATE TABLE CL_PRODMOLD_FISC_1 (
    tid INT,
    code VARCHAR(255),
    unitatea_mas VARCHAR(255),
    vid VARCHAR(255),
    name VARCHAR(1024),
    description VARCHAR(1024),
    format VARCHAR(255),
    weight VARCHAR(50),
    language VARCHAR(50)
);

And more rows like this 

